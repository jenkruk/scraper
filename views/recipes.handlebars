{{!-- Header space 

<div class="row">
    <h3 class="card">{{title}}</h3>
    <h5 class="card-panel recipeBody">{{body}}</h5>
</div>

<div class="row">
    <div class="col s2"></div>
    <form action="/comment/{{recipe._id}}" method="POST" class="card-panel col s8">
        <h5>Comment on this Recipe</h5>
        <div class="row">
            <div class="input-field">
                <i class="material-icons prefix">perm_identity</i>
                <input type="text" placeholder="Name" name="name" id="icon_prefix" class="validate active">
            </div>

            <div class="input-field">
                <i class="material-icons prefix">comment</i>
                <input type="text" placeholder="Comment" name-"comment" id="icon_telephone" class="validate active">
            </div>
        </div>
        <div class="row">
            <div class="input-field">
                <center><input type="submit" value="Submit" class="btn add-comment-button topnav"></center>
            </div>
        </div>
    </form>
</div>

<div class="row">
    <div class="col s2"></div>
    <div class="col s8">
        <h5>Comments:</h5>
        {{#if recipe.comment}}
        {{#each recipe.comment}}
        <div class="card">
            <p id="nameComment">{{name}}</p>
            <p>{{body}}</p>
        </div>
        {{/each}}
        {{else}}
        <p>No comments yet.</p>
        {{/if}}
    </div>
</div> --}}